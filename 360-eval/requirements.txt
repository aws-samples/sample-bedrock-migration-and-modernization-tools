Jinja2==3.1.6
boto3>=1.43.29
# pandas 2.2.3 segfaults on Python 3.14 / numpy 2.5 (no cp314 wheels); 2.3.3+ ships them.
# Capped <3 because streamlit 1.50.0 requires pandas<3.
pandas>=2.3.3,<3
plotly==6.3.1
python-dotenv==1.1.0
litellm[google]==1.82.6
psutil==7.0.0
streamlit==1.54.0
scipy==1.16.2
requests==2.32.4
tenacity==9.1.2
# Web UI (Flask) — needed for the offline single-process app
flask>=2.3.0
flask-cors>=4.0.0
# OFFLINE: local credential encryption (replaces AWS KMS)
cryptography>=42